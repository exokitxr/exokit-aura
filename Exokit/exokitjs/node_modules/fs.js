function readFileSync(path) {
    if (path.includes('http')) {
        let file = File.create(path, 2);
        return file.loadAsText();
    } else {
        let file = File.create(path);
        return file.loadAsText();
    }
}
exports = {
    readFileSync
}
