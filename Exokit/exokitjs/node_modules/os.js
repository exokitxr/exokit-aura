exports = {
    cpus: function() {
        let count = EXOKIT.processorCount;
        let array = [];
        for (let i = 0; i < count; i++) {
            array.push({model: 'iOS CPU'});
        }
        return array;
    }
}
