exports = {
    cpus: function() {
        let count = NodeOSBacking.getProcessorCount();
        let array = [];
        for (let i = 0; i < count; i++) {
            array.push({model: 'iOS CPU'});
        }
        return array;
    }
}
